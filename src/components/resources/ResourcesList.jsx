// Takes an array of Resources as props and map through them to display a list

function ResourcesList(props) {
  return (
    <ul>
      <p>List of Resources</p>
    </ul>
  );
}

export default ResourcesList;
